<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        <small>Data da publicação: {{postagem.data}}</small><br>
        <small>Categoria: {{categoria.nome}}</small>
        <hr>
        {{postagem.conteudo}}
    </div>
</div>